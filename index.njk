---
layout: layout.njk
---

<ul>
  {% for post in collections.post | reverse %}
  <li>
    <a href="{{ post.url | url }}">{{ post.data.title }}</a>
    <small><time datetime="{{ post.date | isoDate }}">{{ post.date | formatDate }}</time></small>
  </li>
  {% endfor %}
</ul>
